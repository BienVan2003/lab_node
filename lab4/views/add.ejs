<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.6.4/dist/jquery.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Add product</title>
</head>
<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-xl-5 col-lg-6 col-md-8 border mt-3 pt-3 rounded">
                <div class="form-group text-primary">
                    <small><a href="/">Back</a></small>
                </div>
                <h4 class="text-primary">Add new product</h4>
                <form class="mt-3" action="" method="post" enctype="multipart/form-data">
                    <div class="form-group">
                        <label for="user">Product name</label>
                        <input value="<%= name %>" class="form-control" type="text" placeholder="Enter product name" name="name" id="name">
                    </div>
                    <div class="form-group">
                        <label for="pass2">Product price</label>
                        <input value="<%= price %>" class="form-control" type="number" placeholder="Enter product price" name="price" id="price">
                    </div>
                    <div class="form-group">
                        <label for="pass2">Product image</label>
                        <div class="custom-file">
                            <input accept="image/*" type="file" class="custom-file-input" name="image" id="customFile">
                            <label for="customFile" class="custom-file-label text-truncate">Select file</label>
                        </div>
                    </div>
                    <div class="form-group">
                        <label for="pass2">Product description</label>
                        <textarea class="form-control" name="desc" id="desc" rows="4"><%= desc %></textarea>
                    </div>
                    <% if(error) { %>
                        <div class="alert alert-danger">
                            <%= error %>
                        </div>
                    <% } %>
                    <div class="form-group">
                        <button class="btn btn-success mr-2">Add product</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
    <script>
        // Hiển thị tên file đã chọn
        $(".custom-file-input").on("change", function() {
            var fileName = $(this).val().split("\\").pop();
            $(this).siblings(".custom-file-label").addClass("selected").html(fileName);
        })
    </script>
</body>
</html>